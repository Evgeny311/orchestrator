apiVersion: v1
kind: Service
metadata:
  name: inventory-app-service
  namespace: microservices
  labels:
    app: inventory-app
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: inventory-app