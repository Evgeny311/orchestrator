apiVersion: v1
kind: Service
metadata:
  name: billing-app-service
  namespace: microservices
  labels:
    app: billing-app
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: billing-app
  clusterIP: None  # Headless service for StatefulSet