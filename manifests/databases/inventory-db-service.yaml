apiVersion: v1
kind: Service
metadata:
  name: inventory-db-service
  namespace: microservices
  labels:
    app: inventory-db
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: inventory-db
  clusterIP: None  # Headless service for StatefulSet