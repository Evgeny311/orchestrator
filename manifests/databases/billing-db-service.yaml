apiVersion: v1
kind: Service
metadata:
  name: billing-db-service
  namespace: microservices
  labels:
    app: billing-db
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: billing-db
  clusterIP: None  # Headless service for StatefulSet